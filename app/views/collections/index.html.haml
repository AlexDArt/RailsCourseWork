%h1 Listing collections

%table
  %thead
    %tr
      %th Name
      %th Description
      %th Start date
      %th End date
      %th Halls
      %th
      %th

  %tbody
    - @collections.each do |collection|
      %tr
        %td= collection.name
        %td= collection.description
        %td= collection.start_date
        %td= collection.end_date
        %td= collection.halls.size
        %td= link_to 'Show', collection
        %td= link_to 'Edit', edit_collection_path(collection)
        %td= link_to 'Destroy', collection, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Collection', new_collection_path
